# Environment Configuration
# Copy this file to .env and fill in your values

# --- App ---
APP_NAME=AI-Workbench
APP_ENV=development
DEBUG=true
LOG_LEVEL=INFO

# --- Server ---
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000
FRONTEND_PORT=5173

# --- Database (PostgreSQL) ---
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=workbench
# Placeholder only - set your own value here, and keep it out of version control.
POSTGRES_PASSWORD=change_me_local_dev_only
POSTGRES_DB=ai_workbench
DATABASE_URL=postgresql+asyncpg://workbench:change_me_local_dev_only@postgres:5432/ai_workbench

# --- Redis ---
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_URL=redis://redis:6379/0

# --- JWT ---
# Must be replaced when APP_ENV is not a development/test value; the app refuses
# to start with this placeholder secret in production. Generate one with:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET_KEY=change-me-to-a-random-secret-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# --- AI Models ---
LLM_PROVIDER=deepseek
LLM_MODEL=deepseek-chat
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# --- Search ---
SEARCH_PROVIDER=mock
SEARCH_API_KEY=
SEARCH_MAX_RESULTS=10

# --- Memory (Obsidian) ---
OBSIDIAN_VAULT_PATH=/path/to/your/obsidian/vault
